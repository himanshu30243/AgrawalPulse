# Copy to `.env` (docker compose loads that filename automatically) next to
# docker-compose.prod.yml and fill in real values before `docker compose -f
# docker-compose.prod.yml up -d --build`. `.env` is gitignored - never commit the real one.
#
# Generate both with: openssl rand -hex 32

POSTGRES_PASSWORD=
LOCAL_JWT_SECRET=
