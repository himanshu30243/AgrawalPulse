package com.agrawalpulse.common.model;

// Duplicated (not moved) from com.agrawalpulse.family.MaritalStatus - see Gender.java in this
// package for why.
public enum MaritalStatus {
    SINGLE,
    MARRIED,
    WIDOWED,
    DIVORCED
}
