# Copy to .env.production, or set these as build-time env vars in the CI/CD
# pipeline for the AWS "prod" environment. Replace the placeholder with the
# prod API Gateway invoke URL / custom domain fronting the ECS services.
VITE_API_BASE_URL=https://api.agrawalpulse.example.org/api/v1
VITE_APP_ENV=prod
