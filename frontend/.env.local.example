# Copy to .env.local for local development against docker-compose backend.
# Vite loads .env.local automatically for `npm run dev` and it is git-ignored,
# so this is safe for local secrets too (none needed today).
VITE_API_BASE_URL=http://localhost:8080/api/v1
VITE_APP_ENV=local
