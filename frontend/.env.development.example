# Copy to .env.development, or set these as build-time env vars in the CI/CD
# pipeline for the AWS "dev" environment. Replace the placeholder with the
# dev API Gateway invoke URL / custom domain fronting the ECS services.
VITE_API_BASE_URL=https://api-dev.agrawalpulse.example.org/api/v1
VITE_APP_ENV=dev
